[pytest]
testpaths = backend/tests tests
norecursedirs = archive .git .venv .env .gemini
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --strict-markers
markers =
    unit: Unit tests
    integration: Integration tests
